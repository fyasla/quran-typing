# Identifiants Supabase (publics : la clé anon est protégée par les politiques RLS).
# Copier en `.env` avec les valeurs du projet (Settings → API) pour activer
# les comptes et la synchronisation. Sans ce fichier, l'app reste 100 % locale.
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
